# PostgreSQL Connection
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=varity

# JWT Secret — must match the CLI's JWT_SECRET
JWT_SECRET=your_jwt_secret_here

# Server
PORT=3001
NODE_ENV=development
