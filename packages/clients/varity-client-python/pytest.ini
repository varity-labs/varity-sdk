[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers
markers =
    integration: Integration tests requiring running gateway
    unit: Unit tests that don't require external services
