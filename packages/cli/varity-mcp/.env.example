# Varity MCP — required environment variables for Akash deployment
# Set all values in Akash Console before deploying. Never commit actual values.

# GitHub token (for varity_create_repo tool)
GITHUB_TOKEN=                    # github PAT with repo scope

# MCP access token
VARITY_MCP_DEV_TOKEN=            # beta access token from Varity admin
