# WARNING: Never hardcode secrets. Use ${VAR_NAME} substitution.
# Values injected at deploy time via Akash Console. See .env.example for required vars.
---
version: "2.0"

services:
  mcp:
    image: ghcr.io/varity-labs/varity-mcp:v1.3.0
    expose:
      - port: 3100
        as: 80
        to:
          - global: true
        accept:
          - mcp.varity.so
        http_options:
          max_body_size: 10485760
    env:
      - NODE_ENV=production
      - VARITY_AUTH_URL=https://auth.varity.so
      - VARITY_GATEWAY_URL=https://varity.app
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - VARITY_MCP_DEV_TOKEN=${VARITY_MCP_DEV_TOKEN}

profiles:
  compute:
    mcp:
      resources:
        cpu:
          units: 0.5
        memory:
          size: 512Mi
        storage:
          size: 1Gi

  placement:
    akash:
      pricing:
        mcp:
          denom: uakt
          amount: 1000

deployment:
  mcp:
    akash:
      profile: mcp
      count: 1
